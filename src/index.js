import React from 'react';
import ReactDOM from 'react-dom/client';
import ZapravkaValue from './zapravkaValue';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ZapravkaValue />
  </React.StrictMode>
);

